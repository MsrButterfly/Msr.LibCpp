#ifndef MSR_NETWORK_PROTOCOL_HPP_INCLUDED
#define MSR_NETWORK_PROTOCOL_HPP_INCLUDED

#include <boost/asio.hpp>

namespace msr {
    namespace network {
        namespace protocol {
            namespace ip = boost::asio::ip;
        }
    }
}

#endif
